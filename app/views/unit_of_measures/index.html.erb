<table class="table table-striped table-with-link-column">
  <thead>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% if @unit_of_measures.each do |unit_of_measure| %>
    <tr>
      <td nowrap><%= unit_of_measure.name %></td>
      <td nowrap>              
        <%= link_to [unit_of_measure, :return_url => return_url(request.original_url)],
                    class: 'btn btn-default btn-icon btn-sm hidden-print' do %>
            <span class="glyphicon glyphicon-eye-open"></span>
        <% end %>
      </td>
    </tr>
  <% end.empty? %>
    <tr>
      <td colspan="2">No unit of measures found</td><td></td>
    </tr>
  <% end %>
  </tbody>
</table>