<table class="table table-striped table-with-link-column">
  <thead>
  <tr>
    <th>Sender</th>
    <th colspan="2">Source</th>
    <th>Unit of Measure</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% if @translations.each do |translation| %>
    <tr>
      <td><%= translation.sender_value %></td>
      <td><%= translation.source_field %></td>
      <td><%= translation.source_value %></td>
      <td nowrap><%= translation.unit_of_measure_display_string %></td>
      <td nowrap>              
        <%= link_to [translation, :return_url => return_url(request.original_url)],
                    class: 'btn btn-default btn-icon btn-sm hidden-print' do %>
            <span class="glyphicon glyphicon-eye-open"></span>
        <% end %>
      </td>
    </tr>
  <% end.empty? %>
    <tr>
      <td colspan="4">No unit of measure translations found</td><td></td>
    </tr>
  <% end %>
  </tbody>
</table>